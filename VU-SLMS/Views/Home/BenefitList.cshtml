@model IEnumerable<VU_SLMS.Models.Benefit>
@{
    ViewData["Title"] = "Benefit List";
}

<div class="card">
    <div class="card-body">
        <h5 class="card-title">@ViewData["Title"]</h5>

        <!-- Table with stripped rows -->
        <table class="table datatable">
            <thead>
                <tr>
                    <th scope="col">Employee Name</th>
                    <th scope="col">Benefit Type</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach(var L in Model){
                <tr>
                    <td>@L.EmployeeId</td>
                    <td>@L.Name</td>
                    <td>
                        <a asp-action="BenefitDetail" asp-route-id="@L.Id" class="btn btn-sm btn-outline-info">Detail</a>
                    </td>
                </tr>
                }
            </tbody>
        </table>
        <!-- End Table with stripped rows -->

    </div>
</div>